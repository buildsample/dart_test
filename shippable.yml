language: python

python:
   - 3.2

before_script:
 - mkdir -p ./shippable/testresults

install: true

script: 
 - cp testresults.xml  ./shippable/testresults
 #- echo "<script>alert("owned");</script>"
 - cat dart.js
 - cat "<script> alert('hey') </script>"
 - echo "<script> alert('helllo') </script>"
#after_archive:
 #     - echo $SHIPPABLE_ARCHIVE_DOWNLOAD_URL
 
 
archive: true
build_image:
  name: shippable/minv2
  net: host
  privileged: false
  
#build_image: shippable/minv2
